---
const currentYear = new Date().getFullYear();
---

<footer class="footer">
  <div class="container">
    <div class="footer-content">
      <div class="footer-section">
        <h3 class="footer-title">Aberturas Sapper</h3>
        <p class="footer-text">
          Fabricación e instalación de aberturas de aluminio a medida.
        </p>
      </div>

      <div class="footer-section">
        <h4 class="footer-subtitle">Navegación</h4>
        <nav class="footer-nav">
          <a href="/">Inicio</a>
          <a href="/productos">Productos</a>
          <a href="/contacto">Contacto</a>
        </nav>
      </div>

      <div class="footer-section">
        <h4 class="footer-subtitle">Contacto</h4>
        <div class="footer-contact">
          <p>Consultas por WhatsApp o email</p>
          <a href="/contacto" class="btn btn-primary">Contactar</a>
        </div>
      </div>
    </div>

    <div class="footer-bottom">
      <p>&copy; {currentYear} Aberturas Sapper. Todos los derechos reservados.</p>
    </div>
  </div>
</footer>

<style>
  .footer {
    background-color: var(--color-primary);
    color: white;
    padding: var(--spacing-2xl) 0 var(--spacing-lg);
    margin-top: auto;
  }

  .footer-content {
    display: grid;
    gap: var(--spacing-xl);
    margin-bottom: var(--spacing-xl);
  }

  .footer-title {
    font-size: var(--font-size-xl);
    margin-bottom: var(--spacing-md);
  }

  .footer-subtitle {
    font-size: var(--font-size-lg);
    margin-bottom: var(--spacing-md);
  }

  .footer-text {
    color: rgba(255, 255, 255, 0.8);
    line-height: 1.6;
  }

  .footer-nav {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-sm);
  }

  .footer-nav a {
    color: rgba(255, 255, 255, 0.8);
    transition: color var(--transition-fast);
  }

  .footer-nav a:hover {
    color: white;
  }

  .footer-contact p {
    color: rgba(255, 255, 255, 0.8);
    margin-bottom: var(--spacing-md);
  }

  .footer-bottom {
    padding-top: var(--spacing-lg);
    border-top: 1px solid rgba(255, 255, 255, 0.2);
    text-align: center;
    color: rgba(255, 255, 255, 0.6);
    font-size: var(--font-size-sm);
  }

  @media (min-width: 768px) {
    .footer-content {
      grid-template-columns: repeat(3, 1fr);
    }
  }
</style>
